public class RecordPatterns {
    public static void main(String[] args) {

        /*var techPerson = new TechPerson("Alice", 25);
        // Match the record pattern

        if (techPerson instanceof TechPerson record(String name, int age)) {

        System.out.println("The techGearShowPerson's name is " + name + " and their age is " + age);

         */
        }
    }
}
